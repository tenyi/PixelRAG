"""PixelRAG umbrella package — the `pixelrag` pipeline CLI.

Capture (stage 0) is the standalone `pixelshot` command. This package provides the
`pixelrag` umbrella that dispatches to the downstream pipeline stages.
"""
